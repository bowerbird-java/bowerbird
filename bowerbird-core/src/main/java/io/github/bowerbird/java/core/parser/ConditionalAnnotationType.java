package io.github.bowerbird.java.core.parser;

/**
 * The type of Bowerbird conditional annotation found on a source element.
 */
public enum ConditionalAnnotationType {
    IF_DEF,
    IF_NOT_DEF,
    ELSE_IF_DEF,
    ELSE_DEF
}
